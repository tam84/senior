   <%= link_to "ver em outro formato", users_path%>
         

              <div class="card">
                <div class="card-header card-header-rose card-header-icon">
                  <div class="card-icon">
                    <i class="material-icons">assignment</i>
                  </div>
                  <h4 class="card-title">Mensagens Recebidas</h4>
                </div>
                <div class="card-body">
                  <div class="table-responsive">
                    <table class="table">
                      <thead>
                        <tr>
                          <th class="text-center">Data</th>
                          <th>Nome</th>                          
                          <th>Empresa</th>
                          <th>Descrição</th>                          
                          <th class="text-right">Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% for user in @users %>



                        <tr>
                          <td class="text-center"><%= h truncate(user.created_at.to_s, :length => 15)%></td>
                          <td ><%= h truncate(user.name, :length => 13) %></td>
                          <td ><%= h truncate(user.firm.name, :length => 13) %></td>                          
                          <td><%= h truncate(user.description, :length => 30) %></td>

                         




                          <td class="td-actions text-right">

                            <button type="button" rel="tooltip" class="btn" data-original-title="" title="">
                              <%= link_to ("<i class='material-icons'>info</i>+info").html_safe, user_path(user), {style:'color:#fff;'}%>

                            </button>
                          </td>
                        </tr>
                        <% end %>


                      </tbody>
                    </table>
                  </div>
                </div>
              </div>