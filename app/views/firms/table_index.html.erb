   <%= link_to "ver em outro formato", firms_path%>
         

              <div class="card">
                <div class="card-header card-header-rose card-header-icon">
                  <div class="card-icon">
                    <i class="material-icons">assignment</i>
                  </div>
                  <h4 class="card-title">Mensagens Recebidas</h4>
                </div>
                <div class="card-body">
                  <div class="table-responsive">
                    <table class="table">
                      <thead>
                        <tr>
                          <th class="text-center">Data</th>
                          <th>Nome</th>
                          <th>Descrição</th>                          
                          <th class="text-right">Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% for firm in @firms %>



                        <tr>
                          <td class="text-center"><%= h truncate(firm.created_at.to_s, :length => 15)%></td>
                          <td ><%= h truncate(firm.name, :length => 13) %></td>
                          <td><%= h truncate(firm.description, :length => 30) %></td>

                         




                          <td class="td-actions text-right">

                            <button type="button" rel="tooltip" class="btn" data-original-title="" title="">
                              <%= link_to ("<i class='material-icons'>info</i>+info").html_safe, firm_path(firm), {style:'color:#fff;'}%>

                            </button>
                          </td>
                        </tr>
                        <% end %>


                      </tbody>
                    </table>
                  </div>
                </div>
              </div>